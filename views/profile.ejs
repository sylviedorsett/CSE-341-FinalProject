<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="css/styles.css" />
    <title>Homepage</title>
  </head>

  <body>
    <nav>
      <ul>
        <% if (user) { %>
        <li><a href="/logout">Log out</a></li>
        <% } else { %>
        <li><a href="/login">Login</a></li>
        <% } %>
        <li><a href="/">Homepage</a></li>
        <li><a href="/profile">Profile</a></li>
        <li><a href="/api-docs">API Docs</a></li>
      </ul>
    </nav>
    <header>
      <h6>Welcome to your profile, <%= user.nickname %></h6>
    </header>
    <main>
      <% if (user){ %>
      <div class="profileInfo">
        <img src="<%= user.picture %>" alt="<%= user.nickname %> picture" />
        <p class="name"><%= user.nickname.charAt(0) %></p>
      </div>

      <% } %>
    </main>
  </body>
</html>
